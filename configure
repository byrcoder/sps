#!/bin/bash

WORK_DIR=`pwd`
BUILD_DIR="${WORK_DIR}/build"
SRT_BUILD=${BUILD_DIR}/srt
ST_BUILD=${BUILD_DIR}/st
GTEST_BUILD=${BUILD_DIR}/gtest

mkdir -p ${BUILD_DIR}

. auto/depends.sh

echo "====== build sps ${BUILD_DIR} ".

cd ${BUILD_DIR} && cmake .. && make

